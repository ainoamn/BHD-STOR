# سجل جلسة التطوير — 2026-07-21 (سياق متعدد الأجهزة)

> **الغرض:** نقل السياق بين أجهزة التطوير دون فقدان ما نوقش وما نُفّذ وما سيُنفَّذ.  
> **المستودع:** https://github.com/ainoamn/BHD-STOR  
> **آخر commits ذات صلة بهذه الجلسة:** انظر `git log` على `main` · دفعة 2026-07-22: مسار بيع (order+COD) + MarketplaceModule  
> **المسار المعتمد على Windows:** `C:\dev\bhd-app` فقط (مسارات عربية تكسر Next.js)

الخطة النشطة دائماً: [`ROADMAP.md`](../ROADMAP.md)  
المراجعة الهندسية: [`FULL_AUDIT.md`](./FULL_AUDIT.md)  
تقارير AI المؤرشفة: [`external-reviews/`](./external-reviews/)

---

## 1) ما هو المشروع (حقيقة واحدة)

**BHD-STOR** = منصة تجارة إلكترونية عمانية متعددة البائعين (B2B/B2C/C2C/hybrid)، **ليس** منصة عقارات.

| الطبقة | التقنية |
|--------|---------|
| Frontend | Next.js 14 App Router · next-intl (ar/en RTL) · Tailwind · React Query · Zustand |
| Backend | NestJS · TypeORM (**ليس Prisma**) · PostgreSQL · Redis · Bull (ليس BullMQ) |
| النمط | Modular Monolith |

جاهزية تقريبية: **~40–55%** إنتاج · UI أعلى · مسار بيع حقيقي أقل.

---

## 2) ما نوقش في هذه المحادثة (ملخص القرارات)

### أ) تشغيل ومسارات
- التشغيل من `C:\dev\bhd-app` / `C:\dev\bhd-run` → ASCII فقط.
- Downloads والمسارات العربية = نسخ احتياطية، لا تشغيل Next منها.

### ب) مراجعات المساعدين الخارجيين
| المصدر | الحكم |
|--------|--------|
| **ChatGPT** | صحيح جوهرياً بعد توفر الملفات؛ أخطأ مبكراً عن «مستودع فارغ»؛ نقاط CD/API صحيحة وأُصلحت |
| **DeepSeek** | تقرير أول جيد ~75–85%؛ رد المتابعة = إعادة لصق نقد Cursor وتأخر عن الدفع |
| **جيميني / كيمي** | **خاطئ** — افترض عقارات BHD-OM بعد فشل جلب GitHub؛ **لا يُنفَّذ** |
| **Cursor** | راجع الكود محلياً؛ نفّذ P0؛ أرشف التقارير |

النصوص الحرفية محفوظة في `docs/external-reviews/`.

### ج) ميزة تنافسية معتمدة: باركود / متسلسل المتجر
- عند فتح متجر يُولَّد `storeSerial` (مثل `BHD26-A1B2C3`) + `storeCode`.
- المسح/الرابط `/ar/s/{serial}` يفتح **متجر البائع فقط**، ليس السوق العام.
- لوحة البائع وصفحة المتجر العامة: تحميل QR / باركود / ملصق / ملف نصي + طباعة.
- موثّق في `ROADMAP.md` §9.

---

## 3) ما نُفّذ ودُفع إلى GitHub في هذه الجلسة

### أمان
- [x] `JwtAuthGuard` + `RolesGuard` كـ `APP_GUARD` عالميين
- [x] `@Public()` للمنتجات/المتاجر/الفئات/العملات/المراجعات العامة، health، CSRF، tracking، payment/WhatsApp webhooks
- [x] حماية HR / CRM / Accounting / Audit بـ Roles(ADMIN…)
- [x] `ENCRYPTION_MASTER_KEY` fail-closed في production (كان سابقاً)

### تجارة / واجهة
- [x] صفحات `/cart` + `/checkout`
- [x] تفعيل `SmartCart` في layout
- [x] متسلسل/باركود المتجر + migration `007` + `/stores/scan/:code` + `/ar/s/[code]` + `StoreBarcodeCard`
- [x] تحميل الملصق (QR / باركود / صورة مركّبة / ملف نصي) + طباعة من لوحة التاجر وصفحة المتجر العامة
- [x] إصلاح بناء `/dashboard/store`: إضافة `hooks/useAnalytics.ts` (`useStoreAnalytics`)
- [x] **مسار بيع API:** ربط checkout → `POST /orders` (+ عنوان مضمّن) → `POST /payments/process` (يشمل COD)
- [x] إصلاح `MarketplaceModule` المفقود + تسجيل Cart/Orders/Wishlist في `OrdersModule`
- [x] migration `008` لأعمدة الدفع/السلة · `rawBody` لـ Stripe webhooks · Demo Mode افتراضي `false` في start scripts
- [x] توحيد أدوار جزئي: `super_admin` + `moderator` + `seller`/`vendor` على FE
- [x] **Webhook دفع → DB:** تحديث `paymentStatus`/`status` على الطلب + أحداث `order.paid` / `order.created` لإنشاء شحنة لوجستية
- [x] تشديد تحقق توقيع PayPal (رفض إن فشل إلا بـ `PAYPAL_SKIP_WEBHOOK_VERIFY`) + تحقق Thawani عند وجود signature
- [x] إصلاح مسار `OrdersService` في اللوجستيات + تكييف إنشاء الشحنة مع شكل الطلب الحالي
- [x] **توحيد User:** `users/entities` يعيد تصدير الكيان الكانوني من `database/entities` + aliases `isActive`/`isEmailVerified`
- [x] **JWT HttpOnly cookies:** `accessToken`/`refreshToken` + `bhd_session` · cookie-parser · JwtStrategy من cookie أو Bearer
- [x] **Same-origin API proxy:** `/api/v1/*` → Nest · افتراضي cookie-only (بدون JWT في localStorage)
- [x] **عمولة على الطلب:** listener على `order.paid`/`order.created` · seed خطة 10%
- [x] **Seed حسابات تجريبية:** customer/seller مفعّلة + `setup-env.bat`
- [x] **باقات البائع:** 4 خطط (البداية/النخبة/التميز/الريادة) + `POST /subscriptions/choose` + صفحة `/dashboard/store/subscription`
- [x] **بوابات الدفع للأدمن:** `GET/PATCH /admin/payments/gateways` · قائمة عامة نشطة · `processPayment` يرفض المعطّلة · تبويب Gateways في `/dashboard/admin/payments` · migration `011`
- [x] **شركات الشحن للأدمن:** `GET/PATCH /admin/shipping/carriers` · `GET /shipping/carriers` نشطة فقط · حاسبة الأسعار من النشطة · تبويب Shipping في Settings · migration `012`
- [x] **Checkout حي:** شحن من carriers النشطة + دفع من gateways النشطة · تقدير رسوم الشحن متوافق Backend/FE
- [x] **إيقاف silent Demo fallback:** منتجات/متاجر/أدمن منتجات+متاجر+طلبات — الأخطاء تظهر بدل بيانات وهمية عند فشل API
- [x] **شحنة لوجستية تلقائية:** EventEmitter فقط (`order.created`/`paid`/`cancelled`/`status_changed`) · `syncShipmentStatusFromOrder` · سكربت `scripts/smoke-buy-path.mjs`
- [x] **WhatsApp `/order` + `/track`:** Resolver حي عبر Orders + Logistics · `POST /whatsapp/simulate` للاختبار المحلي
- [x] **OpenAI fail-safe:** `isConfigured` · timeout · rate limit محلي · أخطاء 503/429 · fallback للمساعد/التضمين · healthCheck صادق
- [x] **Webhooks دفع موقّعة:** Stripe يتطلب rawBody+signature؛ Thawani fail-closed (HMAC timing-safe) إلا بـ `THAWANI_SKIP_WEBHOOK_VERIFY` خارج production؛ أخطاء التوقيع → 400
- [x] **شحن Oman Post / Aramex fail-safe:** `isConfigured` · محاكاة فقط مع `SHIPPING_ALLOW_MOCK` أو خارج production · إنشاء شحنة يرفض في production بدون مفاتيح · أدمن carriers يعرض `configured`/`sandbox`/`mockAllowed`
- [x] **بوابات الدفع soft-boot:** Stripe/PayPal/Thawani/OmanNet/Telr/CCAvenue لا ترمى عند غياب المفاتيح؛ `ensureConfigured` عند الاستدعاء؛ PayPal webhook fail-closed بدون `PAYPAL_WEBHOOK_ID`
- [x] **بناء Backend (SWC):** stubs للوجستيات/middleware/`app.config` + حزم ناقصة؛ `nest build` ناجح؛ `tsc` ما زال فيه أخطاء أنواع backend
- [x] **بناء Frontend:** مكوّنات `tooltip`/`calendar`/`radio-group` · OG بدون `fs` · hooks أدمن · توحيد casing · `next build` ناجح
- [x] **Feature flags للأنظمة الموسّعة:** `FEATURE_*=false` افتراضياً · `/health` يعرض الحالة · CI يبني BE/FE على Node 20
- [x] **Infra + Auth hardening:** `docker-compose.infra.yml` · `setup-env.bat` · simulate واتساب مقفول في production · gateways العامة بلا تشخيص
- [x] **شحن DHL/FedEx/UPS fail-safe:** نفس بوابة المحاكاة الموثّقة · أدمن configured لـ DHL · smoke يدعم Bearer + Cookie
- [x] **Auth cookies + أسرار الإنتاج:** JwtAuthGuard يقبل cookie أو Bearer · `assertProductionSecrets` · Oman Net fail-closed
- [x] **Telr/CCAvenue webhook gates + env tooling:** fail-closed · `check-env` / `fill-env-secrets`
- [x] **إصلاح هوية المستخدم في الطلبات:** `requireRequestUserId` · COD يتحقق من الملكية
- [x] **Health ready + اختبارات:** `GET /health/ready` يفحص Postgres/Redis · unit tests لـ `request-user` و`assertProductionSecrets`
- [x] **توحيد requireRequestUserId:** Cart/Wishlist/Chat/Notifications/Stores/Products/Reviews/Subscriptions + payments
- [x] **Webhook rate limit فعّال:** `ThrottlerGuard` + `ThrottleLevel.WEBHOOK` (120/د) على `POST /payments/webhook/:gateway`
- [x] **Scripts:** `npm run check:env` / `npm run smoke` / `test:security` · smoke يفحص `/health` + `/health/ready`
- [x] **طلبات العميل حية:** `/orders` عبر API · صفحة `/orders/[id]` بعد checkout · تطبيع استجابة القائمة
- [x] **ملكية الطلب:** `findOne`/`history`/`number` للعميل فقط · الدفع (كل البوابات) يتحقق من ملكية الطلب · `RolesGuard`: `super_admin` يستوفي `admin`
- [x] `getOrderHistory` مُنفَّذ من `statusHistory` أو الحالة الحالية
- [x] **مفضلة حية:** محاذاة مسارات FE مع Nest · صفحة `/wishlist` · نقل للسلة
- [x] **طلبات التاجر:** `findAll` يفلتر بـ `storeId` للمالك · `assertOrderAccess` يسمح لصاحب المتجر · FE يرسل `storeId`
- [x] **returnUrl بعد الدفع:** يتضمن locale + `orderId`
- [x] **ملكية الاسترداد:** `createRefund` يسمح للأدمن أو صاحب متجر الطلب فقط
- [x] **تحقق الدفع:** `GET /payments/:id/verify` + محاذاة FE · صفحة `/payments/return`
- [x] إصلاح `gatewayTransactionId` في سجل الدفع
- [x] **منتجات التاجر:** صفحات `/dashboard/store/products/new` و`.../[id]/edit` · `createProduct`/`updateProduct` + hooks · فئات من API
- [x] **تدقيق 2026-07-23:** [`docs/SECURITY-AUDIT-2026-07-23.md`](./SECURITY-AUDIT-2026-07-23.md) · إغلاق IDOR حالة الطلب · تقييد returnUrl · واتساب بلا token ثابت

### توثيق
- [x] تحديث `ROADMAP.md` / `README.md`
- [x] أرشيف `docs/external-reviews/*` (ChatGPT, DeepSeek, Gemini/Kimi, Cursor, DeepSeek follow-up, STATUS)
- [x] هذا الملف: `docs/SESSION-2026-07-21.md`
- [x] تقرير الحماية: `docs/SECURITY-AUDIT-2026-07-23.md`

---

## 4) ما لم يُنفَّذ بعد (التالي على أي جهاز)

مرتّب حسب الأولوية من `ROADMAP.md`:

1. **بيئة:** Docker Desktop → `docker compose -f docker-compose.infra.yml up -d` → `setup-env.bat` → `node scripts/check-env.mjs` → migrations **012** → seed
2. تحقق: `curl http://localhost:3001/health/ready` ثم smoke: `npm run smoke`
3. تفعيل CSRF عالمي / SameSite مناسب · تحقق توقيع Telr · Redis throttle
4. sandbox Stripe/Thawani بمفاتيح حقيقية
5. كوبون في واجهة السلة (hooks موجودة)
6. تنظيف `tsc` backend (اختياري)

---

## 5) كيف تكمل على جهاز آخر (Checklist)

```bat
git clone https://github.com/ainoamn/BHD-STOR.git
cd /d C:\dev\bhd-app
git pull origin main
```

1. اقرأ **هذا الملف** + `ROADMAP.md` + `docs/FULL_AUDIT.md` + `docs/SECURITY-AUDIT-2026-07-23.md`.  
2. انسخ `backend/.env.example` → `.env` و`frontend/.env.example` → `.env.local`.  
3. `PUBLIC_APP_URL` / `NEXT_PUBLIC_APP_URL` لمسارات QR.  
4. `NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1`  
5. Backend: `npm install` → `npm run migration:run` (حتى **012**) → `npm run seed` → `npm run start:dev`  
   أو من الجذر: `setup-env.bat` أولاً.  
6. Frontend: `npm install` → `npm run dev` → `http://localhost:3000/ar`  
   (API عبر `/api/v1` proxy إلى Nest)  
7. لا تشغّل من مجلد عربي.

---

## 6) روابط سريعة على GitHub

| مورد | رابط |
|------|------|
| المستودع | https://github.com/ainoamn/BHD-STOR |
| ROADMAP | https://github.com/ainoamn/BHD-STOR/blob/main/ROADMAP.md |
| FULL_AUDIT | https://github.com/ainoamn/BHD-STOR/blob/main/docs/FULL_AUDIT.md |
| تدقيق الحماية | https://github.com/ainoamn/BHD-STOR/blob/main/docs/SECURITY-AUDIT-2026-07-23.md |
| تقارير AI + STATUS | https://github.com/ainoamn/BHD-STOR/tree/main/docs/external-reviews |
| هذا السجل | https://github.com/ainoamn/BHD-STOR/blob/main/docs/SESSION-2026-07-21.md |

---

## 7) محادثة Cursor (مرجع تقني)

معرّف المحادثة الداخلي (للربط داخل Cursor فقط):  
`3421cca2-b2d7-40da-8e98-07e55ea1d809`

العنوان المقترح: **BHD-STOR session context sync**

---

*آخر تحديث: 2026-07-23 — تدقيق حماية/تشفير/أداء + إصلاحات P0/P1.*
