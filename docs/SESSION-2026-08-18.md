# سجل جلسة التطوير — 2026-08-18 (هوية BHD للمتجر)

> **الغرض:** توثيق ربط المتجر ببوابة الهوية الموحّدة ونقل السياق بين الأجهزة.  
> **المستودع:** https://github.com/ainoamn/BHD-STOR  
> **النطاق الحي:** https://bhdstor.bhd-om.com  
> **الهوية:** https://id.bhd-om.com  
> **فرع العمل:** `main` (مرفوع إلى `origin/main`)

**للجهاز الثاني ابدأ من:** [`HANDOFF-SECOND-PC.md`](./HANDOFF-SECOND-PC.md)  
**تنفيذ المنتج:** [`BHD-STORE-IDENTITY.md`](./BHD-STORE-IDENTITY.md)  
**المواصفة المجمّدة:** [`BHD-IDENTITY-SSO.md`](./BHD-IDENTITY-SSO.md)

---

## 1) القرار

المتجر عميل OIDC `bhd-store` مثل وازن وحسابي: شاشة الدخول هي بوابة الهوية، لا بطاقة سوق محلية. بيانات الطلبات والمحافظ وكلمات المرور المحلية لا تُمس. الحكم العام يبقى **NO-GO**.

---

## 2) ما نُفّذ ودُفع على `main`

| Commit | الموضوع |
|--------|---------|
| `cb0d635` | ربط أولي: PKCE + Nest complete + `users.bhd_sub` (migration 017) |
| `51088ab` | إكمال SSO على `bhdstor` دون إعادة كتابة localhost لـ API |
| `683d68d` | تبادل الرمز على Next + جلسة منتج إن تعذّر Nest |
| `98c401f` | `/auth/login` يحوّل فوراً إلى شاشة `id.bhd-om.com`؛ المحلي على `?local=1` |

مسارات Next: `/api/auth/bhd/start` · `callback` · `logout` · `status`.  
Nest: `POST /api/v1/auth/bhd/complete`. كوكيز المنتج host-only وليست `bhd_id`.

---

## 3) لماذا اختلف شكل شاشة الدخول

`/ar/auth/login` كانت صفحة متجر (قفل + بريد). المواقع الأخرى تحوّل إلى `id.bhd-om.com/login` (قسمان + شعار BHD). بعد `98c401f` المتجر غلاف تحويل كالمواصفة §6.6.

---

## 4) إنتاج Vercel

- المُصدِر: `BHD_IDENTITY_ISSUER=https://id.bhd-om.com`
- `client_id=bhd-store`
- `BACKEND_URL` يجب أن يكون أصل Nest العام إن وُجد؛ وإلا SSO يكتمل على Next فقط (بدون upsert `bhd_sub`)
- لا تضبط redirect على localhost في الإنتاج
- `GET https://bhdstor.bhd-om.com/api/auth/bhd/status` للتشخيص بلا أسرار

---

## 5) التالي

1. انتظار نشر Vercel لـ `98c401f`+ والتأكد أن `/ar/auth/login` يفتح شاشة الهوية.
2. عند توفر Nest عام: `BACKEND_URL` + migration 017 لربط الطلبات بنفس المستخدم.
3. لا ترفع حكم NO-GO. لا تنسخ أسرار الهوية.

---

*فرع `feat/bhd-identity-sso` قديم بالنسبة لـ `main` — لا تُعد دمجه.*
