# حالة المستودع — 2026-07-22

**Repo:** https://github.com/ainoamn/BHD-STOR  
**المسار المحلي:** `C:\dev\bhd-app`

## ما اكتمل في هذه الدفعة

| العنصر | الحالة |
|--------|--------|
| Checkout → `POST /orders` (عنوان مضمّن + عناصر) | ✅ |
| COD عبر `POST /payments/process` | ✅ |
| `MarketplaceModule` + Cart/Orders/Wishlist مسجّلة | ✅ |
| Migration `008-align-orders-checkout` | ✅ |
| Stripe `rawBody: true` | ✅ |
| Demo Mode افتراضي `false` في scripts / `.env.example` | ✅ |
| أدوار: `super_admin` + `moderator` + `seller`/`vendor` | جزئي ✅ |

## التالي

1. `migration:run` (007+008) + seed على PostgreSQL  
2. Webhook دفع يحدّث `payment_status` في DB  
3. شحنة بعد الدفع  
4. توحيد كيان User الواحد  
