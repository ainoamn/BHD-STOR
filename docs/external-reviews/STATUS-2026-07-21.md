# حالة المستودع — 2026-07-22 (proxy + commission + seed)

**Repo:** https://github.com/ainoamn/BHD-STOR

## مكتمل حديثاً

| عنصر | حالة |
|------|------|
| Same-origin `/api/v1` proxy | ✅ |
| Cookie-only auth (افتراضي) | ✅ |
| عمولة تلقائية عند الطلب | ✅ |
| Seed customer/seller + خطة 10% | ✅ |
| `setup-env.bat` | ✅ |

## تشغيل سريع

```bat
setup-env.bat
cd backend && npm run migration:run && npm run seed && npm run start:dev
cd frontend && npm run dev
```

حسابات بعد seed: `customer@bhdoman.com` / `Customer@123!` · `seller@bhdoman.com` / `Seller@123!`
